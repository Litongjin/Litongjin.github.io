source "https://rubygems.org"

# 若要在本地（装了 Ruby 的环境）预览博客，用这个 Gemfile：
#   bundle install
#   bundle exec jekyll serve
# 在 GitHub Pages 上构建时，Pages 会忽略本文件、使用它自己的受支持 gem 版本。
gem "github-pages", group: :jekyll_plugins
